language: php

php:
  - 7.0
  - 7.1
  - hhvm
  - nightly

env:
  matrix:
    - HACK=0

matrix:
  allow_failures:
    - php: nightly
  include:
    - php: hhvm
      env: HACK=1

before_script: "if [ \"$HACK\" = \"1\" ]; then hhvm -v Eval.EnableHipHopSyntax=true; fi"
script: phpunit
